# Скопируйте в .env и заполните значения.
# Файл .env не коммитится в git.

# Username бота без @ (из @BotFather)
TELEGRAM_BOT_USERNAME=YourBotUsername

# Токен бота (из @BotFather)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# Секрет для сессий Flask (в production задайте свой)
SECRET_KEY=your-secret-key

# Для входа с телефона через приложение Telegram: секрет для API create-login-token (задайте случайную строку)
# LOGIN_TOKEN_SECRET=your-random-secret

# Базовый URL сайта (для ссылки «Войти на сайт» в боте). Если пусто — берётся из запроса.
# Пример: https://example.com или http://192.168.1.100:5000
# SITE_BASE_URL=

# Порт сервера (по умолчанию 80 — чтобы origin был http://127.0.0.1 и виджет Telegram не блокировался CSP)
# PORT=5000
