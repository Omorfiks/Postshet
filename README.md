# Витрина мемов

Сайт-витрина для отображения мемов из Telegram канала с системой реакций.

## Возможности

- 📱 Автоматическая загрузка мемов из Telegram канала
- 😀 Система реакций с эмодзи
- 🎬 Поддержка фото и видео
- 🎨 Современный и красивый интерфейс
- ⚡ Анимации эмодзи (проигрываются только 1 раз)
- 📊 Счетчики реакций под каждым постом

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте конфигурацию в `telegram_bot.py`:
   - `BOT_TOKEN` - токен вашего Telegram бота (получите у @BotFather)
   - `CHANNEL_USERNAME` - username канала (например: @memes_channel)
   - `API_BASE_URL` - URL вашего Flask API (по умолчанию: http://localhost:5000/api)

3. Запустите Flask сервер:
```bash
python app.py
```

4. В другом терминале запустите Telegram бота:
```bash
python telegram_bot.py
```

## Использование

1. Откройте браузер и перейдите на `http://localhost:5000`
2. Правый клик по посту открывает панель реакций
3. Клик по счетчику реакции добавляет еще одну реакцию
4. Анимации эмодзи проигрываются только 1 раз при первом добавлении реакции

## Структура проекта

```
Postshet/
├── app.py                 # Flask backend API
├── telegram_bot.py        # Telegram бот для мониторинга канала
├── requirements.txt       # Python зависимости
├── README.md              # Документация
├── static/
│   ├── css/
│   │   └── style.css      # Стили
│   └── js/
│       └── app.js         # JavaScript логика
├── templates/
│   └── index.html         # Главная страница
├── database/
│   └── memes.db           # База данных SQLite (создается автоматически)
└── uploads/               # Папка для загруженных медиа файлов
```

## Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Добавьте бота в канал как администратора
4. Убедитесь, что бот имеет права на чтение сообщений
5. Укажите токен и username канала в `telegram_bot.py`

## Вход через Telegram (кнопка «Войти» в шапке)

Чтобы работала кнопка входа и отображение никнейма после привязки:

1. Создайте файл `.env` в корне проекта (скопируйте из `.env.example`) и заполните:
   - `TELEGRAM_BOT_USERNAME` — username бота без @ (тот же бот, что в `telegram_bot.py`, или другой)
   - `TELEGRAM_BOT_TOKEN` — токен этого бота (для проверки подписи)
   - `SECRET_KEY` — секрет Flask для сессий (в production обязательно свой ключ)

   После этого достаточно запускать `python app.py` — переменные подхватятся из `.env`.

2. В @BotFather: **Bot Settings → Domain** укажите домен вашего сайта (например `127.0.0.1` или `localhost`). Без этого виджет не будет работать.

3. По умолчанию сервер запускается на **порту 80** — открывайте сайт как **http://127.0.0.1** (без :5000). Так origin совпадает с политикой Telegram (frame-ancestors), и виджет в popup не блокируется. Если порт 80 недоступен (например, на Windows без прав администратора), задайте в `.env` строку `PORT=5000` и откройте http://127.0.0.1:5000 — в этом случае возможна ошибка CSP в окне входа; для её устранения используйте порт 80 или хост без порта в production.

4. **Вход с телефона**: на мобильном устройстве кнопка «Войти» открывает приложение Telegram (чат с ботом). Пользователь нажимает «Войти на сайт» в сообщении бота и попадает на сайт уже авторизованным. Для этого в `.env` задайте `LOGIN_TOKEN_SECRET` (любая случайная строка, одна и та же для сайта и бота) и при необходимости `SITE_BASE_URL` (URL, по которому открывается сайт с телефона, например `https://ваш-домен.ru` или `http://192.168.1.100:5000`). Бот должен быть запущен (`python telegram_bot.py`).

## Особенности

- Анимации эмодзи ограничены одним циклом воспроизведения
- Автоматическое обновление постов каждые 30 секунд
- Адаптивный дизайн для мобильных устройств
- Header с аватаром и названием Telegram канала
